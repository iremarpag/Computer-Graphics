enum MENU_TYPE {
  MENU_SHAPE,
  MENU_MODE,
  MENU_COLOR
};

enum SHAPES {
  SHAPE_SPHERE,
  SHAPE_CUBE,
  SHAPE_BUNNY,
};

enum DRAWING_MODE {
  DRAWING_MODE_WIREFRAME,
  DRAWING_MODE_SOLID
};

enum COLOR {
  COLOR_LSD,
  COLOR_BLACK
};
